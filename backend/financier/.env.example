# Copy this file to `.env` (git-ignored). Docker Compose reads it automatically.

# Application database
DB_USERNAME=postgres
DB_PASSWORD=admin
DB_NAME=postgres

# SonarQube database
SONAR_DB_USERNAME=sonar
SONAR_DB_PASSWORD=sonar

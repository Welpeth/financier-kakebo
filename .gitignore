# IDEs e Ferramentas (Geral)
.idea/
out/
*.iml

# Backend (Spring Boot / Maven)
backend/target/
backend/.idea/
backend/out/

# Secrets / local-only config (NEVER commit)
.env
**/.env
!**/.env.example
**/application-local.properties

# Frontend (Node / React / Angular)
frontend/node_modules/
frontend/.idea/
frontend/dist/
frontend/build/